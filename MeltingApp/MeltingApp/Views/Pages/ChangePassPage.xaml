<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:fields="clr-namespace:MeltingApp.Behaviors"
             x:Class="MeltingApp.Views.Pages.ChangePassPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Change Password" HorizontalOptions="CenterAndExpand" />
            <Label Text="Old password:"></Label>
            <Entry Text="{Binding OldPass}" IsPassword="True">
                <Entry.Behaviors>
                    <fields:FieldsLowerCaseBehavior></fields:FieldsLowerCaseBehavior>
                </Entry.Behaviors>
            </Entry>
            <Label Text="New password:"></Label>
            <Entry Text="{Binding NewPass}" IsPassword="True">
                <Entry.Behaviors>
                    <fields:FieldsLowerCaseBehavior></fields:FieldsLowerCaseBehavior>
                </Entry.Behaviors>
            </Entry>
            <Button Text="Save" Command="{Binding ChangePassCommand}"></Button>
            <Label Text="{Binding ResponseMessage}"></Label>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>