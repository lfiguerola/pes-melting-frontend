<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MeltingApp.Views.Pages.HelpPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Frequently Asked" HorizontalTextAlignment="Center" VerticalOptions="Center" FontSize="Medium" FontAttributes="Bold"/>

            <BoxView  Color="Blue" WidthRequest="10" HeightRequest="10"></BoxView>


                <Label Text=" 1.What is Karma?" ></Label>

                <Label Text=" 2.How to find people from your university?" ></Label>

                <Label Text=" 3.Privacy " ></Label>
                
                
                

            
            
            
            <!--  <ListView  x:Name="ListViewHelp" ItemsSource="{Binding ListElements}"  SelectedItem="{Binding HelpElement}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell  Text="{Binding Nombre}" Detail="{Binding Descripcion}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
                
            </ListView> -->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>