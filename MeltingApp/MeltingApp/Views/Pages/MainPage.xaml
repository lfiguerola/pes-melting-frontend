<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MeltingApp.Views.Pages.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to Main Page!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Button Text="Create Event" Command="{Binding NavigateToCreateEventPageCommand}"></Button>
            <Button x:Name="ViewButton" Text="View Profile" Command="{Binding ViewProfileCommand}"></Button>
            <Button Text="Create Profile" Command="{Binding NavigateToCreateProfilePageCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>