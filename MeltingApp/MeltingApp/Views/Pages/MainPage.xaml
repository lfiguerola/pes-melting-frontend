<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MeltingApp.Views.Pages.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to Main Page!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />


            <Button Text="Static Info" Command="{Binding NavigateToStaticInfoPage}"></Button>
            <Button Text="Finder" Command="{Binding NavigateToFinderPage}"></Button>
            <Button Text="Create Event" Command="{Binding NavigateToCreateEventPageCommand}"></Button>
            <Button Text="Get All Events" Command="{Binding NavigateToEventViewModelCommand}"></Button>
            <Button Text="View Event" Command="{Binding NavigateToViewEventPageCommand}"></Button>
            <Button x:Name="ViewButton" Text="View Profile" Command="{Binding ViewProfileCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>